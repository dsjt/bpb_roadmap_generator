# ビルドロードマップジェネレーター 仕様書

## 概要

BackpackBattlesのビルド進行を「ラウンド × アイテム」のグリッドで可視化するツール。
アイテムをキャンバス上に配置して図を作成・エクスポートできる。

## 環境

- 静的HTML/CSS/JS（`index.html` + `style.css` + `script.js` に分割）
- GitHub Pagesで公開
- サーバー不要、ブラウザ内で完結
- 外部ライブラリ：`html2canvas`（CDN経由、PNGエクスポートに使用）

## キャンバス

- やや横長の固定レイアウト
- 行：1行固定（シングルロードマップ）
- 列：ラウンド1〜18の18列固定、列ヘッダーにラウンド番号を表示
- 1セルに複数アイテムを配置可能（同ラウンドで複数アイテムを取得するケースに対応）

## アイテムパレット

アイテムの種類は2種類：

1. **定義済みアイテム**：BackpackBattlesのアイテム名一覧（別途提供）をパレットに表示
2. **カスタムアイテム**：ユーザーがテキスト入力して任意の名前でアイテムを作成できる

パレット上のアイテムはデフォルトでアイテム名のテキストオブジェクトとして表示される。

## 画像アップロード

- アイテムごとに画像をアップロードできる
- 画像が紐付けられているアイテムはキャンバス・パレット上で画像表示に切り替わる（テキストの代わり）

## 配置操作

- パレットからキャンバスのセルへドラッグ＆ドロップで配置
- セル上の配置済みアイテムをクリックで削除

## エクスポート・保存・読み込み

**PNGエクスポート**
- キャンバス部分をPNGとしてダウンロード（html2canvas使用）

**URL共有**
- 配置状態をエンコードしてURLパラメータに埋め込む
- そのURLを開くと配置が自動復元される（インポート機能を兼ねる）
- 文字列はエンコード/デコードできればよく、人間が直接編集することは想定しない

**自動保存**
- 配置状態をlocalStorageに自動保存する
- ページ再読み込み後も最後の状態を復元する

## 優先度の低い機能（MVP後）

- 複数行対応（マルチビルド比較）
- 行・列の動的な追加・削除
- セルへのテキストメモ追加
- 配置のundo/redo
